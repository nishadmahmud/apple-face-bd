export const metadata = {
  title: "Sign In | Apple Face",
  description: "Log in or create an Apple Face account to manage orders and your profile.",
};

export default function LoginLayout({ children }) {
  return children;
}
