export const metadata = {
    title: 'Track Your Order | Apple Face',
    description: 'Track the real-time status of your order at Apple Face using your invoice ID.',
};

export default function TrackOrderLayout({ children }) {
    return <>{children}</>;
}
